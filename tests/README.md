# Tests

Tests will be split into hardware-independent protocol/unit tests and
hardware-in-the-loop validation. No test should require proprietary vendor
material to be committed.

Record hardware/firmware revisions and exact test conditions for every HIL run.
