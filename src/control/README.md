# A203 control

Reserved for the documented A203 control plane.

Initial code should support read-only identity/status and injectable transport
adapters before configuration writes are enabled. Every implemented command
must cite the applicable A203 protocol document and version in code comments or
tests. BF01 register addresses and UDP-demo commands are not A203 requirements
without vendor confirmation.
