# USB bridge firmware

Reserved for bridge firmware compatible with the legacy AudioXtreamer
host-facing USB protocol. The supporting hardware is part of the carrier plan,
but this firmware is not part of the core A203 milestone.

See `docs/usb-bridge.md` for feasibility gates and `docs/provenance.md` before
bringing in any legacy source.
